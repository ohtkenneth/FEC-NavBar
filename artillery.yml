config:
  target: 'http://localhost:5000'
  # payload:
  #   path: "./test/testArtillery.csv"
  #   fields:
  #     - "id"
  #     - "location"
  #     - "size"
  #     - "brand"
  #     - "season"
  #     - "url"
  phases:
    - duration: 60
      arrivalRate: 500
scenarios:
  - flow:
    - get:
        url: "/product/ads"
    # - get:
    #     url: "/"
    # - post:
    #     url: "/product/ads"
    #     json:
    #       id: "{{ id }}"
    #       location: "{{ location }}"
    #       size: "{{ size }}"
    #       brand: "{{ brand }}"
    #       season: "{{ season }}"
    #       url: "{{ url }}"


