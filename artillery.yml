config:
  target: 'http://localhost:5000'
  # target: 'http://localhost:8000'
  # payload:
  #   path: "./test/testArtillery.csv"
  #   fields:
  #     - "id"
  #     - "location"
  #     - "size"
  #     - "brand"
  #     - "season"
  #     - "url"
  phases:
    - duration: 60
      arrivalRate: 30
scenarios:
  - flow:
    - loop:
      - get:
          url: "/product/ads"
      count: 20
    # - get:
    #     url: "/"
    # - post:
    #     url: "/product/ads"
    #     json:
    #       id: "{{ id }}"
    #       location: "{{ location }}"
    #       size: "{{ size }}"
    #       brand: "{{ brand }}"
    #       season: "{{ season }}"
    #       url: "{{ url }}"


